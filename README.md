# RENGLO SYSTEM

This repository is being merged into `renglo-api` for local development flow.

## Where the guide moved

Use `dev/renglo-api/README.md` as the onboarding and setup guide.

It now contains:

- full environment setup steps
- local config template usage (`env_config.py.TEMPLATE`)
- extension installation flow
- backend and frontend runtime commands

## Local config template location

Use:

- `dev/renglo-api/env_config.py.TEMPLATE`

Local config file should be created in `renglo-api` as:

- `dev/renglo-api/env_config.py`

